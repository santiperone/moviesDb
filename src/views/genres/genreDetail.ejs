<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head') %>
<body>
    <%- include('../partials/header') %>
    <main>
        <h1><%= genre.name %> </h1>
        <ul>
            <li>Ranking: <%= genre.ranking %></li>             
            <li>Películas: 
                <ul>
                    <% genre.Movies.forEach(movie => { %>
                    <li><a href="/movies/detail/<%= movie.id %>"><%= movie.title %></a></li>                  
                    <% }) %>
                </ul>
            </li>         
        </ul>
        
    </main>

    <%- include('../partials/footer') %> 
</body>
</html>